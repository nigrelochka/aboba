def setup():
    size(600,600)
def draw():       
    for f in range(0,625,50):
        for g in range(0,625,50):
            ellipse(g,f,50,50)
            fill(random(0,255),random(0,255),random(0,255))
